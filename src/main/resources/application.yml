openapi:
  name: ${spring.application.name}
  title: BillingDataCollector
  version: '@project.version@'

spring:
  application:
    name: api-billing-data-collector
  jpa:
    hibernate:
      ddl-auto: validate
